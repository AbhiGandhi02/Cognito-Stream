# Flask web framework
flask==3.0.0
flask-cors==4.0.0

# Manim animation engine
manim==0.18.0

# Scientific computing
numpy==1.26.2
scipy==1.11.4

# Image processing
pillow==9.5.0

# Audio processing
pydub==0.25.1

# Text-to-Speech (local, open-source)
piper-tts==1.2.0

# Supabase Storage client (for uploading rendered mp4/mp3 to cloud bucket).
# Optional: if SUPABASE_SERVICE_ROLE_KEY is unset, the renderer falls back
# to local filesystem and the import is unused.
supabase==2.7.4

# Production server
gunicorn==21.2.0

# Monitoring
psutil==5.9.6

# Utilities
python-dotenv==1.0.0